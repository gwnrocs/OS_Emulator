# OS_Emulator

LAPUZ, Salvador <br>
ORTIZ, China<br>
ROCO, Gwen Kathleen<br>
YAO, Benson<br>
<br>
Entry class file: main.cpp<br>
<br>
Instructions to run:
1. Within the project folder (same directory as .cpp, .h, .sln files), create another folder named "Logs".
2. Open "[CSOPESY] MO1.sln"
3. Type "initialize"
4. Enter any of the following valid commands: “exit”, “screen -s <process_name", "screen -r <process_name>", "screen -ls", "scheduler -test", "scheduler -stop", "report -util"
5. To exit, use the command, "exit"
